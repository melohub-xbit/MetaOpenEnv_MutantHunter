{
  "repo": "bloom_filter_lite",
  "module": "bloom_filter_lite.bloom",
  "module_relpath": "bloom.py",
  "total_mutants": 84,
  "killed": 28,
  "baseline_mutation_score": 0.333333,
  "coverage_baseline": 78.7097,
  "surviving_mutants": [
    {
      "mutant_id": "AOR-22-14-0",
      "operator": "AOR",
      "line": 22,
      "column": 14,
      "original": "Mult",
      "mutated": "FloorDiv",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 // LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-21-15-1",
      "operator": "NCR",
      "line": 21,
      "column": 15,
      "original": "2",
      "mutated": "3",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(3)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "LCR-39-7-3",
      "operator": "LCR",
      "line": 39,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) and isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "LCR-43-7-5",
      "operator": "LCR",
      "line": 43,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) and isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "LCR-47-7-6",
      "operator": "LCR",
      "line": 47,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 and error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-50-7-7",
      "operator": "ROR",
      "line": 50,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m <= 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-53-7-8",
      "operator": "ROR",
      "line": 53,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k <= 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-41-19-9",
      "operator": "NCR",
      "line": 41,
      "column": 19,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 1:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-47-7-10",
      "operator": "ROR",
      "line": 47,
      "column": 7,
      "original": "LtE",
      "mutated": "Lt",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate < 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-47-28-11",
      "operator": "ROR",
      "line": 47,
      "column": 28,
      "original": "GtE",
      "mutated": "Gt",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate > 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-50-11-12",
      "operator": "NCR",
      "line": 50,
      "column": 11,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 2:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-50-11-13",
      "operator": "NCR",
      "line": 50,
      "column": 11,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 0:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-51-12-14",
      "operator": "NCR",
      "line": 51,
      "column": 12,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 2\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-51-12-15",
      "operator": "NCR",
      "line": 51,
      "column": 12,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 0\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-53-11-16",
      "operator": "NCR",
      "line": 53,
      "column": 11,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 2:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-53-11-17",
      "operator": "NCR",
      "line": 53,
      "column": 11,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 0:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-54-12-18",
      "operator": "NCR",
      "line": 54,
      "column": 12,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 2\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-54-12-19",
      "operator": "NCR",
      "line": 54,
      "column": 12,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 0\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-71-58-20",
      "operator": "NCR",
      "line": 71,
      "column": 58,
      "original": "0.01",
      "mutated": "1.01",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=1.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-71-58-21",
      "operator": "NCR",
      "line": 71,
      "column": 58,
      "original": "0.01",
      "mutated": "0.0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.0) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-103-11-23",
      "operator": "ROR",
      "line": 103,
      "column": 11,
      "original": "LtE",
      "mutated": "Lt",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m < 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-111-15-25",
      "operator": "AOR",
      "line": 111,
      "column": 15,
      "original": "Div",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones * self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "ROR-151-11-26",
      "operator": "ROR",
      "line": 151,
      "column": 11,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 != 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "LCR-185-11-30",
      "operator": "LCR",
      "line": 185,
      "column": 11,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m and self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-196-24-31",
      "operator": "AOR",
      "line": 196,
      "column": 24,
      "original": "Add",
      "mutated": "Sub",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count - other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-213-22-32",
      "operator": "NCR",
      "line": 213,
      "column": 22,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 1\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-47-42-34",
      "operator": "NCR",
      "line": 47,
      "column": 42,
      "original": "1.0",
      "mutated": "2.0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 2.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-49-22-36",
      "operator": "AOR",
      "line": 49,
      "column": 22,
      "original": "Div",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) * LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-52-18-37",
      "operator": "AOR",
      "line": 52,
      "column": 18,
      "original": "Mult",
      "mutated": "FloorDiv",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity // LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-77-31-38",
      "operator": "AOR",
      "line": 77,
      "column": 31,
      "original": "FloorDiv",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) * 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-103-22-39",
      "operator": "NCR",
      "line": 103,
      "column": 22,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 1:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-104-19-40",
      "operator": "NCR",
      "line": 104,
      "column": 19,
      "original": "0.0",
      "mutated": "1.0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 1.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-151-17-41",
      "operator": "NCR",
      "line": 151,
      "column": 17,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 1:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-152-17-42",
      "operator": "NCR",
      "line": 152,
      "column": 17,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 2\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-152-17-43",
      "operator": "NCR",
      "line": 152,
      "column": 17,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 0\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-212-31-47",
      "operator": "AOR",
      "line": 212,
      "column": 31,
      "original": "FloorDiv",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) * 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-49-22-48",
      "operator": "AOR",
      "line": 49,
      "column": 22,
      "original": "Mult",
      "mutated": "FloorDiv",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity // math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-52-19-49",
      "operator": "AOR",
      "line": 52,
      "column": 19,
      "original": "Div",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m * capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-25-53",
      "operator": "NCR",
      "line": 139,
      "column": 25,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(2, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-25-54",
      "operator": "NCR",
      "line": 139,
      "column": 25,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(0, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-139-28-55",
      "operator": "AOR",
      "line": 139,
      "column": 28,
      "original": "FloorDiv",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) * 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-154-17-56",
      "operator": "AOR",
      "line": 154,
      "column": 17,
      "original": "Add",
      "mutated": "Sub",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 - i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-212-32-64",
      "operator": "AOR",
      "line": 212,
      "column": 32,
      "original": "Add",
      "mutated": "Sub",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m - 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-212-48-65",
      "operator": "NCR",
      "line": 212,
      "column": 48,
      "original": "8",
      "mutated": "9",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 9)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-77-36-67",
      "operator": "NCR",
      "line": 77,
      "column": 36,
      "original": "7",
      "mutated": "8",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 8) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-139-29-69",
      "operator": "AOR",
      "line": 139,
      "column": 29,
      "original": "Add",
      "mutated": "Sub",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() - 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-55-70",
      "operator": "NCR",
      "line": 139,
      "column": 55,
      "original": "8",
      "mutated": "9",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 9)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-55-71",
      "operator": "NCR",
      "line": 139,
      "column": 55,
      "original": "8",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 0)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "BCR-140-55-72",
      "operator": "BCR",
      "line": 140,
      "column": 55,
      "original": "True",
      "mutated": "False",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=False)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "AOR-154-22-73",
      "operator": "AOR",
      "line": 154,
      "column": 22,
      "original": "Mult",
      "mutated": "FloorDiv",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i // h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-212-42-74",
      "operator": "NCR",
      "line": 212,
      "column": 42,
      "original": "7",
      "mutated": "8",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 8) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-212-42-75",
      "operator": "NCR",
      "line": 212,
      "column": 42,
      "original": "7",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 0) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-49-76",
      "operator": "NCR",
      "line": 139,
      "column": 49,
      "original": "8",
      "mutated": "9",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 9) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-139-49-77",
      "operator": "NCR",
      "line": 139,
      "column": 49,
      "original": "8",
      "mutated": "0",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 0) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-147-52-78",
      "operator": "NCR",
      "line": 147,
      "column": 52,
      "original": "8",
      "mutated": "9",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=9, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=8, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    },
    {
      "mutant_id": "NCR-148-52-80",
      "operator": "NCR",
      "line": 148,
      "column": 52,
      "original": "8",
      "mutated": "9",
      "mutated_source": "\"\"\"Small bit-array Bloom filter.\n\nHand-rolled \u2014 no third-party Bloom library, no ``bitarray`` dependency. The\narithmetic for ``m`` (bits) and ``k`` (hash functions) is laid out in plain\nPython so mutation testing can bite into every constant and operator.\n\nReferences:\n    * Burton H. Bloom, \"Space/Time Trade-offs in Hash Coding with Allowable\n      Errors\" (1970).\n    * Standard sizing formulas:\n          m = ceil(-n * ln(p) / (ln 2)^2)\n          k = round((m / n) * ln 2)\n\"\"\"\nfrom __future__ import annotations\nimport hashlib\nimport math\nfrom typing import Any\nLN2 = math.log(2)\nLN2_SQUARED = LN2 * LN2\n\nclass BloomFilterError(ValueError):\n    \"\"\"Raised on invalid Bloom-filter arguments or shape-mismatched merges.\"\"\"\n\ndef optimal_parameters(capacity: int, error_rate: float) -> tuple[int, int]:\n    \"\"\"Return ``(m, k)`` \u2014 bit-array size and number of hash functions.\n\n    Args:\n        capacity: Expected number of distinct elements ``n``.\n        error_rate: Target false-positive probability ``p`` in ``(0, 1)``.\n\n    Both bounds are exclusive. ``m`` is at least 1 and ``k`` is at least 1\n    even for tiny inputs that would otherwise round to zero.\n    \"\"\"\n    if not isinstance(capacity, int) or isinstance(capacity, bool):\n        raise BloomFilterError(f'capacity must be int, got {type(capacity).__name__}')\n    if capacity <= 0:\n        raise BloomFilterError(f'capacity must be positive, got {capacity}')\n    if not isinstance(error_rate, (int, float)) or isinstance(error_rate, bool):\n        raise BloomFilterError(f'error_rate must be a real number, got {type(error_rate).__name__}')\n    if error_rate <= 0.0 or error_rate >= 1.0:\n        raise BloomFilterError(f'error_rate must be in (0, 1), got {error_rate}')\n    m = int(math.ceil(-capacity * math.log(error_rate) / LN2_SQUARED))\n    if m < 1:\n        m = 1\n    k = int(round(m / capacity * LN2))\n    if k < 1:\n        k = 1\n    return (m, k)\n\nclass BloomFilter:\n    \"\"\"A space-efficient probabilistic set.\n\n    Membership queries are exact for negatives but allow false positives \u2014\n    never false negatives. The filter is sized at construction from a\n    target ``capacity`` and ``error_rate`` and never resizes.\n\n    Two filters are *shape-compatible* when their bit count ``m`` and hash\n    count ``k`` are equal; only shape-compatible filters can be merged.\n    \"\"\"\n    __slots__ = ('_capacity', '_error_rate', '_m', '_k', '_bits', '_count')\n\n    def __init__(self, capacity: int, error_rate: float=0.01) -> None:\n        m, k = optimal_parameters(capacity, error_rate)\n        self._capacity = capacity\n        self._error_rate = float(error_rate)\n        self._m = m\n        self._k = k\n        self._bits = bytearray((m + 7) // 8)\n        self._count = 0\n\n    @property\n    def capacity(self) -> int:\n        return self._capacity\n\n    @property\n    def target_error_rate(self) -> float:\n        return self._error_rate\n\n    @property\n    def bit_size(self) -> int:\n        return self._m\n\n    @property\n    def num_hashes(self) -> int:\n        return self._k\n\n    @property\n    def saturation(self) -> float:\n        \"\"\"Fraction of bits in ``[0, m)`` that are currently set.\"\"\"\n        if self._m <= 0:\n            return 0.0\n        ones = 0\n        for byte in self._bits:\n            ones += bin(byte).count('1')\n        return ones / self._m\n\n    @property\n    def false_positive_rate(self) -> float:\n        \"\"\"Estimated FPR given current saturation: ``saturation ** k``.\"\"\"\n        return self.saturation ** self._k\n\n    def __len__(self) -> int:\n        \"\"\"Number of times :meth:`add` has been called.\n\n        Note this counts duplicates \u2014 a Bloom filter cannot tell them apart\n        from genuinely new elements, so the value is an upper bound on the\n        true number of distinct items inserted.\n        \"\"\"\n        return self._count\n\n    @staticmethod\n    def _encode(item: Any) -> bytes:\n        if isinstance(item, bytes):\n            return item\n        if isinstance(item, str):\n            return item.encode('utf-8')\n        if isinstance(item, bool):\n            return b'\\x01' if item else b'\\x00'\n        if isinstance(item, int):\n            length = max(1, (item.bit_length() + 8) // 8)\n            return item.to_bytes(length, 'big', signed=True)\n        return repr(item).encode('utf-8')\n\n    def _hash_indices(self, item: Any) -> list[int]:\n        data = self._encode(item)\n        digest1 = hashlib.blake2b(data, digest_size=8, person=b'blf-h1--').digest()\n        digest2 = hashlib.blake2b(data, digest_size=9, person=b'blf-h2--').digest()\n        h1 = int.from_bytes(digest1, 'big')\n        h2 = int.from_bytes(digest2, 'big')\n        if h2 == 0:\n            h2 = 1\n        m = self._m\n        return [(h1 + i * h2) % m for i in range(self._k)]\n\n    def add(self, item: Any) -> None:\n        \"\"\"Insert ``item`` into the filter.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            self._bits[byte_idx] |= 1 << bit_idx\n        self._count += 1\n\n    def __contains__(self, item: Any) -> bool:\n        \"\"\"Membership test. May return false positives, never false negatives.\"\"\"\n        for idx in self._hash_indices(item):\n            byte_idx, bit_idx = divmod(idx, 8)\n            if not self._bits[byte_idx] & 1 << bit_idx:\n                return False\n        return True\n\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter':\n        \"\"\"Return a new filter representing the union of ``self`` and ``other``.\n\n        Raises :class:`BloomFilterError` if the two filters have different\n        bit counts ``m`` or hash counts ``k``.\n        \"\"\"\n        if not isinstance(other, BloomFilter):\n            raise BloomFilterError(f'can only merge with BloomFilter, got {type(other).__name__}')\n        if self._m != other._m or self._k != other._k:\n            raise BloomFilterError(f'cannot merge: shape mismatch (m={self._m}, k={self._k}) vs (m={other._m}, k={other._k})')\n        merged = BloomFilter.__new__(BloomFilter)\n        merged._capacity = max(self._capacity, other._capacity)\n        merged._error_rate = max(self._error_rate, other._error_rate)\n        merged._m = self._m\n        merged._k = self._k\n        merged._bits = bytearray((a | b for a, b in zip(self._bits, other._bits)))\n        merged._count = self._count + other._count\n        return merged\n\n    def copy(self) -> 'BloomFilter':\n        \"\"\"Return an independent copy of this filter.\"\"\"\n        c = BloomFilter.__new__(BloomFilter)\n        c._capacity = self._capacity\n        c._error_rate = self._error_rate\n        c._m = self._m\n        c._k = self._k\n        c._bits = bytearray(self._bits)\n        c._count = self._count\n        return c\n\n    def clear(self) -> None:\n        \"\"\"Reset all bits to zero and the insertion counter to zero.\"\"\"\n        self._bits = bytearray((self._m + 7) // 8)\n        self._count = 0\n\n    def __repr__(self) -> str:\n        return f'BloomFilter(capacity={self._capacity}, error_rate={self._error_rate}, m={self._m}, k={self._k}, count={self._count})'\n__all__ = ['BloomFilter', 'BloomFilterError', 'optimal_parameters']"
    }
  ],
  "module_summary": "Small bit-array Bloom filter.\nclass BloomFilterError(ValueError):\n    '''Raised on invalid Bloom-filter arguments or shape-mismatched merges.'''\ndef optimal_parameters(capacity: int, error_rate: float): ...\nclass BloomFilter:\n    '''A space-efficient probabilistic set.'''\n    def __init__(self, capacity: int, error_rate: float = 0.01) -> None: ...\n    @property\n    def capacity(self) -> int: ...\n    @property\n    def target_error_rate(self) -> float: ...\n    @property\n    def bit_size(self) -> int: ...\n    @property\n    def num_hashes(self) -> int: ...\n    @property\n    def saturation(self) -> float: ...\n    @property\n    def false_positive_rate(self) -> float: ...\n    def __len__(self) -> int: ...\n    def add(self, item: Any) -> None: ...\n    def __contains__(self, item: Any) -> bool: ...\n    def merge(self, other: 'BloomFilter') -> 'BloomFilter': ...\n    def copy(self) -> 'BloomFilter': ...\n    def clear(self) -> None: ...\n    def __repr__(self) -> str: ...",
  "existing_test_names": [
    "tests/test_bloom.py::test_optimal_parameters_returns_two_positive_ints",
    "tests/test_bloom.py::test_add_then_contains_true",
    "tests/test_bloom.py::test_missing_item_not_contained",
    "tests/test_bloom.py::test_len_after_two_adds",
    "tests/test_bloom.py::test_capacity_property",
    "tests/test_bloom.py::test_target_error_rate_property",
    "tests/test_bloom.py::test_bit_size_positive",
    "tests/test_bloom.py::test_num_hashes_positive",
    "tests/test_bloom.py::test_saturation_zero_when_empty",
    "tests/test_bloom.py::test_saturation_increases_after_add",
    "tests/test_bloom.py::test_copy_contains_same_item",
    "tests/test_bloom.py::test_merge_union_membership",
    "tests/test_bloom.py::test_clear_removes_membership",
    "tests/test_bloom.py::test_invalid_capacity_raises"
  ]
}
