{
  "repo": "interval_tree",
  "module": "interval_tree.tree",
  "module_relpath": "tree.py",
  "total_mutants": 50,
  "killed": 21,
  "baseline_mutation_score": 0.42,
  "coverage_baseline": 77.8689,
  "surviving_mutants": [
    {
      "mutant_id": "ROR-32-7-0",
      "operator": "ROR",
      "line": 32,
      "column": 7,
      "original": "GtE",
      "mutated": "Gt",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start > end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-38-11-2",
      "operator": "ROR",
      "line": 38,
      "column": 11,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start <= b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-38-31-3",
      "operator": "ROR",
      "line": 38,
      "column": 31,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start <= a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-72-14-4",
      "operator": "NCR",
      "line": 72,
      "column": 14,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 1\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-178-11-10",
      "operator": "ROR",
      "line": 178,
      "column": 11,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) != 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "BCR-184-15-11",
      "operator": "BCR",
      "line": 184,
      "column": 15,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return True\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-75-15-12",
      "operator": "ROR",
      "line": 75,
      "column": 15,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) >= (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-77-19-13",
      "operator": "NCR",
      "line": 77,
      "column": 19,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 2\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-139-19-16",
      "operator": "NCR",
      "line": 139,
      "column": 19,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 1\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-145-15-18",
      "operator": "ROR",
      "line": 145,
      "column": 15,
      "original": "LtE",
      "mutated": "Lt",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s < cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "BCR-171-19-19",
      "operator": "BCR",
      "line": 171,
      "column": 19,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return True\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "BCR-177-19-22",
      "operator": "BCR",
      "line": 177,
      "column": 19,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return True\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-178-24-23",
      "operator": "NCR",
      "line": 178,
      "column": 24,
      "original": "3",
      "mutated": "4",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 4:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-178-24-24",
      "operator": "NCR",
      "line": 178,
      "column": 24,
      "original": "3",
      "mutated": "0",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 0:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "BCR-183-19-25",
      "operator": "BCR",
      "line": 183,
      "column": 19,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return True\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-144-39-33",
      "operator": "NCR",
      "line": 144,
      "column": 39,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[0:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-146-19-34",
      "operator": "ROR",
      "line": 146,
      "column": 19,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e >= cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "LCR-175-19-35",
      "operator": "LCR",
      "line": 175,
      "column": 19,
      "original": "and",
      "mutated": "or",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start or e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "LCR-181-19-36",
      "operator": "LCR",
      "line": 181,
      "column": 19,
      "original": "and",
      "mutated": "or",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start or e == end or p == payload:\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-181-19-40",
      "operator": "ROR",
      "line": 181,
      "column": 19,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s != start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-181-34-41",
      "operator": "ROR",
      "line": 181,
      "column": 34,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e != end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "ROR-181-47-42",
      "operator": "ROR",
      "line": 181,
      "column": 47,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p != payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "BCR-182-27-43",
      "operator": "BCR",
      "line": 182,
      "column": 27,
      "original": "True",
      "mutated": "False",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return False\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-75-20-44",
      "operator": "NCR",
      "line": 75,
      "column": 20,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[1], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-75-28-45",
      "operator": "NCR",
      "line": 75,
      "column": 28,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[2]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-75-28-46",
      "operator": "NCR",
      "line": 75,
      "column": 28,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[0]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-142-62-47",
      "operator": "NCR",
      "line": 142,
      "column": 62,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[1], it[1]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-142-69-48",
      "operator": "NCR",
      "line": 142,
      "column": 69,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[2]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    },
    {
      "mutant_id": "NCR-142-69-49",
      "operator": "NCR",
      "line": 142,
      "column": 69,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Half-open ``[start, end)`` interval container.\n\nA flat, list-backed implementation \u2014 *not* a balanced augmented tree. The\nshape is wrong for asymptotic performance, but right for mutation testing:\nevery comparison, boundary, and overlap predicate sits in plain Python where\nmutmut can flip it.\n\nConventions:\n    * Intervals are half-open: ``start`` is inclusive, ``end`` is exclusive.\n    * ``start < end`` is required; zero-width intervals are rejected.\n    * ``start`` and ``end`` may be ``int`` or ``float``; mixing is fine as\n      long as the comparison is well defined.\n    * \"Overlap\" means strictly positive intersection length.\n      ``[1, 3)`` and ``[3, 5)`` do **not** overlap.\n    * Containers are ordered by ``(start, end, payload-id)`` for stable\n      iteration; equal intervals with different payloads are kept distinct.\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Any, Iterator, Tuple\nInterval = Tuple[float, float, Any]\n\nclass IntervalError(ValueError):\n    \"\"\"Raised on invalid interval arguments (e.g. ``start >= end``).\"\"\"\n\ndef _validate(start: float, end: float) -> None:\n    if start >= end:\n        raise IntervalError(f'start must be < end, got [{start}, {end})')\n\ndef _overlaps(a_start: float, a_end: float, b_start: float, b_end: float) -> bool:\n    \"\"\"True iff two half-open intervals overlap by a strictly positive length.\"\"\"\n    return a_start < b_end and b_start < a_end\n\ndef _contains_point(start: float, end: float, point: float) -> bool:\n    \"\"\"True iff ``point`` lies in the half-open interval ``[start, end)``.\"\"\"\n    return start <= point < end\n\nclass IntervalTree:\n    \"\"\"Container of half-open intervals with overlap / point queries.\n\n    The implementation stores intervals in a list kept sorted by\n    ``(start, end)``. ``add`` is ``O(n)`` (insertion + shift); queries are\n    ``O(n)``. Stability is intentional for mutation-testing leverage \u2014\n    callers see deterministic iteration order.\n    \"\"\"\n    __slots__ = ('_items',)\n\n    def __init__(self) -> None:\n        self._items: list[Interval] = []\n\n    def add(self, start: float, end: float, payload: Any=None) -> None:\n        \"\"\"Insert ``[start, end) -> payload`` into the tree.\n\n        Duplicates (same ``start``, ``end``, and ``payload``) are allowed\n        and stored as separate entries. Insertion preserves the\n        ``(start, end)`` sort order.\n        \"\"\"\n        _validate(start, end)\n        new_item: Interval = (start, end, payload)\n        idx = 0\n        while idx < len(self._items):\n            cur = self._items[idx]\n            if (cur[0], cur[1]) > (start, end):\n                break\n            idx += 1\n        self._items.insert(idx, new_item)\n\n    def remove(self, start: float, end: float, payload: Any=None) -> bool:\n        \"\"\"Remove the first matching ``[start, end)`` (with matching payload).\n\n        Returns True if an item was removed, False otherwise. Only one\n        matching entry is removed per call, even if duplicates exist.\n        \"\"\"\n        _validate(start, end)\n        for idx, (s, e, p) in enumerate(self._items):\n            if s == start and e == end and (p == payload):\n                del self._items[idx]\n                return True\n        return False\n\n    def clear(self) -> None:\n        \"\"\"Drop all stored intervals.\"\"\"\n        self._items = []\n\n    def query_point(self, point: float) -> list[Interval]:\n        \"\"\"Return all intervals that contain ``point``, in insertion-sort order.\"\"\"\n        return [(s, e, p) for s, e, p in self._items if _contains_point(s, e, point)]\n\n    def query_range(self, start: float, end: float) -> list[Interval]:\n        \"\"\"Return all intervals that overlap ``[start, end)`` (positive intersection).\"\"\"\n        _validate(start, end)\n        return [(s, e, p) for s, e, p in self._items if _overlaps(s, e, start, end)]\n\n    def overlaps(self, start: float, end: float) -> bool:\n        \"\"\"True iff any stored interval overlaps ``[start, end)``.\"\"\"\n        _validate(start, end)\n        for s, e, _ in self._items:\n            if _overlaps(s, e, start, end):\n                return True\n        return False\n\n    def total_length(self) -> float:\n        \"\"\"Sum of ``end - start`` across all stored intervals (with duplicates).\"\"\"\n        return sum((e - s for s, e, _ in self._items))\n\n    def merge_overlapping(self) -> int:\n        \"\"\"Merge contiguous / overlapping intervals in place. Payloads are dropped.\n\n        Two intervals are considered mergeable when they overlap *or* touch\n        (``a.end == b.start``). The merged interval keeps payload ``None``.\n        Returns the number of intervals removed by merging (i.e. the\n        reduction in ``len(self)``).\n        \"\"\"\n        if not self._items:\n            return 0\n        before = len(self._items)\n        merged: list[Interval] = []\n        items_sorted = sorted(self._items, key=lambda it: (it[0], it[0]))\n        cur_start, cur_end, _ = items_sorted[0]\n        for s, e, _p in items_sorted[1:]:\n            if s <= cur_end:\n                if e > cur_end:\n                    cur_end = e\n            else:\n                merged.append((cur_start, cur_end, None))\n                cur_start, cur_end = (s, e)\n        merged.append((cur_start, cur_end, None))\n        self._items = merged\n        return before - len(self._items)\n\n    def __len__(self) -> int:\n        return len(self._items)\n\n    def __iter__(self) -> Iterator[Interval]:\n        return iter(list(self._items))\n\n    def __contains__(self, item: object) -> bool:\n        \"\"\"Membership test.\n\n        * ``(start, end)`` checks for any stored interval with that exact span.\n        * ``(start, end, payload)`` checks the full triple.\n        * Anything else returns ``False``.\n        \"\"\"\n        if not isinstance(item, tuple):\n            return False\n        if len(item) == 2:\n            start, end = item\n            for s, e, _p in self._items:\n                if s == start and e == end:\n                    return True\n            return False\n        if len(item) == 3:\n            start, end, payload = item\n            for s, e, p in self._items:\n                if s == start and e == end and (p == payload):\n                    return True\n            return False\n        return False\n\n    def __repr__(self) -> str:\n        return f'IntervalTree({self._items!r})'\n__all__ = ['Interval', 'IntervalError', 'IntervalTree']"
    }
  ],
  "module_summary": "Half-open ``[start, end)`` interval container.\nclass IntervalError: ...\ndef _validate(start, end): ...\ndef _overlaps(a_start, a_end, b_start, b_end): ...\ndef _contains_point(start, end, point): ...\nclass IntervalTree: ...",
  "existing_test_names": [
    "tests/test_tree.py::test_add_then_len",
    "tests/test_tree.py::test_query_point_inside",
    "tests/test_tree.py::test_query_point_outside",
    "tests/test_tree.py::test_query_range_overlap",
    "tests/test_tree.py::test_overlaps_true",
    "tests/test_tree.py::test_overlaps_false",
    "tests/test_tree.py::test_remove_existing_returns_true",
    "tests/test_tree.py::test_remove_missing_returns_false",
    "tests/test_tree.py::test_clear",
    "tests/test_tree.py::test_iter_yields_all",
    "tests/test_tree.py::test_contains_two_tuple",
    "tests/test_tree.py::test_total_length_simple",
    "tests/test_tree.py::test_merge_overlapping_two_overlap",
    "tests/test_tree.py::test_merge_overlapping_disjoint_unchanged"
  ]
}
