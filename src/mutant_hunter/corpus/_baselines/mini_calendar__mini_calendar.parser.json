{
  "repo": "mini_calendar",
  "module": "mini_calendar.parser",
  "module_relpath": "parser.py",
  "total_mutants": 336,
  "killed": 173,
  "baseline_mutation_score": 0.514881,
  "coverage_baseline": 78.5714,
  "surviving_mutants": [
    {
      "mutant_id": "NCR-22-11-0",
      "operator": "NCR",
      "line": 22,
      "column": 11,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 2\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-22-11-1",
      "operator": "NCR",
      "line": 22,
      "column": 11,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 0\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-23-11-2",
      "operator": "NCR",
      "line": 23,
      "column": 11,
      "original": "9999",
      "mutated": "10000",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 10000\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-43-6",
      "operator": "NCR",
      "line": 26,
      "column": 43,
      "original": "28",
      "mutated": "29",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-43-7",
      "operator": "NCR",
      "line": 26,
      "column": 43,
      "original": "28",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 0, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-47-8",
      "operator": "NCR",
      "line": 26,
      "column": 47,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 32, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-47-9",
      "operator": "NCR",
      "line": 26,
      "column": 47,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 0, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-55-12",
      "operator": "NCR",
      "line": 26,
      "column": 55,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 32, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-55-13",
      "operator": "NCR",
      "line": 26,
      "column": 55,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 0, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-59-14",
      "operator": "NCR",
      "line": 26,
      "column": 59,
      "original": "30",
      "mutated": "31",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 31, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-63-16",
      "operator": "NCR",
      "line": 26,
      "column": 63,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 32, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-63-17",
      "operator": "NCR",
      "line": 26,
      "column": 63,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 0, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-67-18",
      "operator": "NCR",
      "line": 26,
      "column": 67,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 32, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-67-19",
      "operator": "NCR",
      "line": 26,
      "column": 67,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 0, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-71-20",
      "operator": "NCR",
      "line": 26,
      "column": 71,
      "original": "30",
      "mutated": "31",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 31, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-71-21",
      "operator": "NCR",
      "line": 26,
      "column": 71,
      "original": "30",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 0, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-75-22",
      "operator": "NCR",
      "line": 26,
      "column": 75,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 32, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-75-23",
      "operator": "NCR",
      "line": 26,
      "column": 75,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 0, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-79-24",
      "operator": "NCR",
      "line": 26,
      "column": 79,
      "original": "30",
      "mutated": "31",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 31, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-79-25",
      "operator": "NCR",
      "line": 26,
      "column": 79,
      "original": "30",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 0, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-83-26",
      "operator": "NCR",
      "line": 26,
      "column": 83,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 32)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-26-83-27",
      "operator": "NCR",
      "line": 26,
      "column": 83,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 0)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-37-28",
      "operator": "NCR",
      "line": 27,
      "column": 37,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (32, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-37-29",
      "operator": "NCR",
      "line": 27,
      "column": 37,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (0, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-41-30",
      "operator": "NCR",
      "line": 27,
      "column": 41,
      "original": "29",
      "mutated": "30",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 30, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-41-31",
      "operator": "NCR",
      "line": 27,
      "column": 41,
      "original": "29",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 0, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-45-32",
      "operator": "NCR",
      "line": 27,
      "column": 45,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 32, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-45-33",
      "operator": "NCR",
      "line": 27,
      "column": 45,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 0, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-49-34",
      "operator": "NCR",
      "line": 27,
      "column": 49,
      "original": "30",
      "mutated": "31",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 31, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-49-35",
      "operator": "NCR",
      "line": 27,
      "column": 49,
      "original": "30",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 0, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-53-36",
      "operator": "NCR",
      "line": 27,
      "column": 53,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 32, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-27-53-37",
      "operator": "NCR",
      "line": 27,
      "column": 53,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30,

      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 274, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-32-77-70",
      "operator": "NCR",
      "line": 32,
      "column": 77,
      "original": "273",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 0, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-32-82-71",
      "operator": "NCR",
      "line": 32,
      "column": 82,
      "original": "304",
      "mutated": "305",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 305, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-32-82-72",
      "operator": "NCR",
      "line": 32,
      "column": 82,
      "original": "304",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 0, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-32-87-73",
      "operator": "NCR",
      "line": 32,
      "column": 87,
      "original": "334",
      "mutated": "335",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 335)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-32-87-74",
      "operator": "NCR",
      "line": 32,
      "column": 87,
      "original": "334",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 0)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-38-76",
      "operator": "NCR",
      "line": 33,
      "column": 38,
      "original": "31",
      "mutated": "32",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 32, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-38-77",
      "operator": "NCR",
      "line": 33,
      "column": 38,
      "original": "31",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 0, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-42-78",
      "operator": "NCR",
      "line": 33,
      "column": 42,
      "original": "60",
      "mutated": "61",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 61, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-42-79",
      "operator": "NCR",
      "line": 33,
      "column": 42,
      "original": "60",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 0, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-46-80",
      "operator": "NCR",
      "line": 33,
      "column": 46,
      "original": "91",
      "mutated": "92",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 92, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-46-81",
      "operator": "NCR",
      "line": 33,
      "column": 46,
      "original": "91",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 0, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-50-82",
      "operator": "NCR",
      "line": 33,
      "column": 50,
      "original": "121",
      "mutated": "122",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 122, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-50-83",
      "operator": "NCR",
      "line": 33,
      "column": 50,
      "original": "121",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 0, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-55-84",
      "operator": "NCR",
      "line": 33,
      "column": 55,
      "original": "152",
      "mutated": "153",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 153, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-60-86",
      "operator": "NCR",
      "line": 33,
      "column": 60,
      "original": "182",
      "mutated": "183",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 183, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-65-88",
      "operator": "NCR",
      "line": 33,
      "column": 65,
      "original": "213",
      "mutated": "214",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 214, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-70-90",
      "operator": "NCR",
      "line": 33,
      "column": 70,
      "original": "244",
      "mutated": "245",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 245, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-75-92",
      "operator": "NCR",
      "line": 33,
      "column": 75,
      "original": "274",
      "mutated": "275",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 275, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-80-94",
      "operator": "NCR",
      "line": 33,
      "column": 80,
      "original": "305",
      "mutated": "306",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 306, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-33-85-96",
      "operator": "NCR",
      "line": 33,
      "column": 85,
      "original": "335",
      "mutated": "336",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 336)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-53-11-100",
      "operator": "ROR",
      "line": 53,
      "column": 11,
      "original": "Eq",
      "mutated": "NotEq",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 != 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-61-7-101",
      "operator": "LCR",
      "line": 61,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 and month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-70-7-103",
      "operator": "LCR",
      "line": 70,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR and year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-72-7-104",
      "operator": "LCR",
      "line": 72,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 and month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-74-7-105",
      "operator": "LCR",
      "line": 74,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 and day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-99-7-108",
      "operator": "LCR",
      "line": 99,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' and s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-139-7-110",
      "operator": "LCR",
      "line": 139,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR and year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "LCR-142-7-111",
      "operator": "LCR",
      "line": 142,
      "column": 7,
      "original": "or",
      "mutated": "and",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 and ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-174-7-119",
      "operator": "ROR",
      "line": 174,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd <= 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-217-7-123",
      "operator": "ROR",
      "line": 217,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m <= 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "AOR-221-8-125",
      "operator": "AOR",
      "line": 221,
      "column": 8,
      "original": "FloorDiv",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y * 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "AOR-298-18-131",
      "operator": "AOR",
      "line": 298,
      "column": 18,
      "original": "Add",
      "mutated": "Sub",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd - (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "AOR-51-7-138",
      "operator": "AOR",
      "line": 51,
      "column": 7,
      "original": "Mod",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year * 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-51-21-139",
      "operator": "NCR",
      "line": 51,
      "column": 21,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 1:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "AOR-53-11-141",
      "operator": "AOR",
      "line": 53,
      "column": 11,
      "original": "Mod",
      "mutated": "Mult",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year * 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-53-25-142",
      "operator": "NCR",
      "line": 53,
      "column": 25,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 1\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-61-20-144",
      "operator": "ROR",
      "line": 61,
      "column": 20,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month >= 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-64-15-146",
      "operator": "NCR",
      "line": 64,
      "column": 15,
      "original": "29",
      "mutated": "30",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 30\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-64-15-147",
      "operator": "NCR",
      "line": 64,
      "column": 15,
      "original": "29",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 0\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "AOR-65-30-148",
      "operator": "AOR",
      "line": 65,
      "column": 30,
      "original": "Sub",
      "mutated": "Add",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month + 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-70-7-149",
      "operator": "ROR",
      "line": 70,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year <= MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-70-26-150",
      "operator": "ROR",
      "line": 70,
      "column": 26,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year >= MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "BCR-71-15-151",
      "operator": "BCR",
      "line": 71,
      "column": 15,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return True\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-72-20-153",
      "operator": "ROR",
      "line": 72,
      "column": 20,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month >= 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "BCR-73-15-154",
      "operator": "BCR",
      "line": 73,
      "column": 15,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return True\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-74-18-156",
      "operator": "ROR",
      "line": 74,
      "column": 18,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day >= days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "BCR-75-15-157",
      "operator": "BCR",
      "line": 75,
      "column": 15,
      "original": "False",
      "mutated": "True",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return True\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-139-7-162",
      "operator": "ROR",
      "line": 139,
      "column": 7,
      "original": "Lt",
      "mutated": "LtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year <= MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "ROR-139-26-163",
      "operator": "ROR",
      "line": 139,
      "column": 26,
      "original": "Gt",
      "mutated": "GtE",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year >= MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-141-19-164",
      "operator": "NCR",
      "line": 141,
      "column": 19,
      "original": "366",
      "mutated": "367",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 367 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-141-50-166",
      "operator": "NCR",
      "line": 141,
      "column": 50,
      "original": "365",
      "mutated": "366",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 366\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-141-50-167",
      "operator": "NCR",
      "line": 141,
      "column": 50,
      "original": "365",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 ==

      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 0 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-142-17-248",
      "operator": "NCR",
      "line": 142,
      "column": 17,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 2 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-142-17-249",
      "operator": "NCR",
      "line": 142,
      "column": 17,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 0 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-148-18-250",
      "operator": "NCR",
      "line": 148,
      "column": 18,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 2 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-148-18-251",
      "operator": "NCR",
      "line": 148,
      "column": 18,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 0 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-168-46-255",
      "operator": "NCR",
      "line": 168,
      "column": 46,
      "original": "400",
      "mutated": "401",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 401\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-252-25-269",
      "operator": "NCR",
      "line": 252,
      "column": 25,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 2)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-252-25-270",
      "operator": "NCR",
      "line": 252,
      "column": 25,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 0)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-258-36-271",
      "operator": "NCR",
      "line": 258,
      "column": 36,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -2)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-258-36-272",
      "operator": "NCR",
      "line": 258,
      "column": 36,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -0)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-272-23-273",
      "operator": "NCR",
      "line": 272,
      "column": 23,
      "original": "0",
      "mutated": "1",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 1 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-272-31-274",
      "operator": "NCR",
      "line": 272,
      "column": 31,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -2\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-272-31-275",
      "operator": "NCR",
      "line": 272,
      "column": 31,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -0\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-298-31-276",
      "operator": "NCR",
      "line": 298,
      "column": 31,
      "original": "3",
      "mutated": "4",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (4 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-298-31-277",
      "operator": "NCR",
      "line": 298,
      "column": 31,
      "original": "3",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (0 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-168-35-292",
      "operator": "NCR",
      "line": 168,
      "column": 35,
      "original": "100",
      "mutated": "101",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 101 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-260-26-299",
      "operator": "NCR",
      "line": 260,
      "column": 26,
      "original": "1",
      "mutated": "2",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -2)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-260-26-300",
      "operator": "NCR",
      "line": 260,
      "column": 26,
      "original": "1",
      "mutated": "0",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -0)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-103-22-304",
      "operator": "NCR",
      "line": 103,
      "column": 22,
      "original": "5",
      "mutated": "6",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[6:7])\n        day = int(s[8:10])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    },
    {
      "mutant_id": "NCR-104-22-310",
      "operator": "NCR",
      "line": 104,
      "column": 22,
      "original": "10",
      "mutated": "11",
      "mutated_source": "\"\"\"Proleptic Gregorian calendar helpers.\n\nPure Python, no `datetime` dependency. Working in raw ``(year, month, day)``\ninteger tuples is the point \u2014 it gives mutation testing a wide surface of\narithmetic, comparisons, and bounds to bite into.\n\nConventions:\n    * ``Date`` is ``(year, month, day)`` with ``1 <= year <= 9999``,\n      ``1 <= month <= 12``, ``1 <= day <= days_in_month(year, month)``.\n    * Day-of-week is Monday=0..Sunday=6 (matches ``datetime.date.weekday``).\n    * \"Julian day\" here means the proleptic Gregorian day count starting\n      at 1 for ``(1, 1, 1)``. It is *not* the astronomical Julian Day Number.\n    * ISO 8601 weeks (Monday-start, week 1 contains the first Thursday).\n\"\"\"\nfrom __future__ import annotations\nfrom typing import Tuple\nDate = Tuple[int, int, int]\nMIN_YEAR = 1\nMAX_YEAR = 9999\n_MONTH_DAYS_NORMAL: tuple[int, ...] = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_MONTH_DAYS_LEAP: tuple[int, ...] = (31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)\n_CUM_DAYS_NORMAL: tuple[int, ...] = (0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334)\n_CUM_DAYS_LEAP: tuple[int, ...] = (0, 31, 60, 91, 121, 152, 182, 213, 244, 274, 305, 335)\nWEEKDAYS: tuple[str, ...] = ('Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun')\n\ndef is_leap_year(year: int) -> bool:\n    \"\"\"Return True iff ``year`` is a Gregorian leap year.\n\n    A year is a leap year iff it is divisible by 4, except centuries that\n    are not divisible by 400. So 2000 is a leap year, 1900 is not.\n    \"\"\"\n    if year % 4 != 0:\n        return False\n    if year % 100 != 0:\n        return True\n    return year % 400 == 0\n\ndef days_in_month(year: int, month: int) -> int:\n    \"\"\"Return the number of days in ``(year, month)``.\n\n    Raises ``ValueError`` when ``month`` is outside ``1..12``.\n    \"\"\"\n    if month < 1 or month > 12:\n        raise ValueError(f'month must be in 1..12, got {month}')\n    if month == 2 and is_leap_year(year):\n        return 29\n    return _MONTH_DAYS_NORMAL[month - 1]\n\ndef is_valid_date(year: int, month: int, day: int) -> bool:\n    \"\"\"Return True iff ``(year, month, day)`` is a valid in-range Gregorian date.\"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        return False\n    if month < 1 or month > 12:\n        return False\n    if day < 1 or day > days_in_month(year, month):\n        return False\n    return True\n\ndef _require_valid(year: int, month: int, day: int) -> None:\n    if not is_valid_date(year, month, day):\n        raise ValueError(f'invalid date: {year:04d}-{month:02d}-{day:02d}')\n\ndef parse_iso_date(s: str) -> Date:\n    \"\"\"Parse a canonical ISO 8601 calendar date ``YYYY-MM-DD``.\n\n    Accepts only the 10-character form. The function does not strip\n    whitespace; callers should normalise the input first.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f'parse_iso_date expects str, got {type(s).__name__}')\n    if len(s) != 10:\n        raise ValueError(f'expected 10 chars (YYYY-MM-DD), got {len(s)}')\n    if s[4] != '-' or s[7] != '-':\n        raise ValueError(f'expected dashes at positions 4 and 7, got {s!r}')\n    try:\n        year = int(s[0:4])\n        month = int(s[5:7])\n        day = int(s[8:11])\n    except ValueError as exc:\n        raise ValueError(f'non-numeric components in {s!r}') from exc\n    _require_valid(year, month, day)\n    return (year, month, day)\n\ndef format_iso(year: int, month: int, day: int) -> str:\n    \"\"\"Format ``(year, month, day)`` as ``YYYY-MM-DD``.\"\"\"\n    _require_valid(year, month, day)\n    return f'{year:04d}-{month:02d}-{day:02d}'\n\ndef ordinal_day(year: int, month: int, day: int) -> int:\n    \"\"\"Return the 1-indexed day of year for ``(year, month, day)``.\n\n    Examples: ``ordinal_day(2024, 1, 1) == 1``,\n    ``ordinal_day(2024, 3, 1) == 61`` (leap),\n    ``ordinal_day(2023, 3, 1) == 60`` (non-leap).\n    \"\"\"\n    _require_valid(year, month, day)\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    return cum[month - 1] + day\n\ndef from_ordinal(year: int, ordinal: int) -> Date:\n    \"\"\"Inverse of :func:`ordinal_day`.\n\n    Given ``(year, day_of_year)``, return the ``(Y, M, D)`` triple.\n    \"\"\"\n    if year < MIN_YEAR or year > MAX_YEAR:\n        raise ValueError(f'year out of range: {year}')\n    days_in_year = 366 if is_leap_year(year) else 365\n    if ordinal < 1 or ordinal > days_in_year:\n        raise ValueError(f'ordinal {ordinal} outside 1..{days_in_year} for year {year}')\n    cum = _CUM_DAYS_LEAP if is_leap_year(year) else _CUM_DAYS_NORMAL\n    month = 12\n    while month > 1 and cum[month - 1] >= ordinal:\n        month -= 1\n    day = ordinal - cum[month - 1]\n    return (year, month, day)\n\ndef to_julian_day(year: int, month: int, day: int) -> int:\n    \"\"\"Days since the proleptic-Gregorian epoch ``(1, 1, 1)`` (which has JD 1).\n\n    Used as the canonical integer representation for date arithmetic. The\n    function is monotonic: ``to_julian_day(d2) > to_julian_day(d1)`` iff\n    ``d2`` is strictly after ``d1``.\n    \"\"\"\n    _require_valid(year, month, day)\n    y = year - 1\n    days = y * 365 + y // 4 - y // 100 + y // 400\n    return days + ordinal_day(year, month, day)\n\ndef from_julian_day(jd: int) -> Date:\n    \"\"\"Inverse of :func:`to_julian_day`.\"\"\"\n    if jd < 1:\n        raise ValueError(f'julian day must be >= 1, got {jd}')\n    n400, jd_mod = divmod(jd - 1, 146097)\n    year = n400 * 400 + 1\n    while True:\n        days_y = 366 if is_leap_year(year) else 365\n        if jd_mod < days_y:\n            return from_ordinal(year, jd_mod + 1)\n        jd_mod -= days_y\n        year += 1\n\ndef add_days(year: int, month: int, day: int, delta: int) -> Date:\n    \"\"\"Return the date ``delta`` days after ``(year, month, day)``.\n\n    ``delta`` may be negative or zero. The result is validated to lie\n    within the supported year range.\n    \"\"\"\n    return from_julian_day(to_julian_day(year, month, day) + delta)\n\ndef date_diff(d1: Date, d2: Date) -> int:\n    \"\"\"Signed day difference: ``to_julian_day(d2) - to_julian_day(d1)``.\"\"\"\n    return to_julian_day(*d2) - to_julian_day(*d1)\n\ndef day_of_week(year: int, month: int, day: int) -> int:\n    \"\"\"Return the day of week as Monday=0..Sunday=6.\n\n    Implemented via Zeller's congruence (Gregorian variant) so that it\n    cross-checks the Julian-day path \u2014 mutating either pathway in\n    isolation will diverge them.\n    \"\"\"\n    _require_valid(year, month, day)\n    m = month\n    y = year\n    if m < 3:\n        m += 12\n        y -= 1\n    K = y % 100\n    J = y // 100\n    h = (day + 13 * (m + 1) // 5 + K + K // 4 + J // 4 + 5 * J) % 7\n    return (h + 5) % 7\n\ndef weekday_name(year: int, month: int, day: int) -> str:\n    \"\"\"Return the three-letter English weekday name.\"\"\"\n    return WEEKDAYS[day_of_week(year, month, day)]\n\ndef is_weekend(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date falls on a Saturday or Sunday.\"\"\"\n    return day_of_week(year, month, day) >= 5\n\ndef is_business_day(year: int, month: int, day: int) -> bool:\n    \"\"\"True iff the date is Monday-through-Friday.\"\"\"\n    return not is_weekend(year, month, day)\n\ndef next_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the first business day strictly after ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, 1)\n    while is_weekend(*d):\n        d = add_days(*d, 1)\n    return d\n\ndef previous_business_day(year: int, month: int, day: int) -> Date:\n    \"\"\"Return the last business day strictly before ``(year, month, day)``.\"\"\"\n    d = add_days(year, month, day, -1)\n    while is_weekend(*d):\n        d = add_days(*d, -1)\n    return d\n\ndef business_days_between(d1: Date, d2: Date) -> int:\n    \"\"\"Count business days strictly between ``d1`` (exclusive) and ``d2`` (inclusive).\n\n    Negative when ``d2`` is before ``d1``. Zero when ``d1 == d2``.\n    \"\"\"\n    diff = date_diff(d1, d2)\n    if diff == 0:\n        return 0\n    step = 1 if diff > 0 else -1\n    count = 0\n    cur = d1\n    for _ in range(abs(diff)):\n        cur = add_days(*cur, step)\n        if is_business_day(*cur):\n            count += step\n    return count\n\ndef iso_week_number(year: int, month: int, day: int) -> int:\n    \"\"\"Return the ISO 8601 week number (1..53) for ``(year, month, day)``.\n\n    A week's \"ISO year\" is determined by the year of its Thursday.\n    Week 1 of an ISO year is the week containing January 4 of that year\n    (equivalently, the first week with at least 4 days in the new year).\n    \"\"\"\n    _require_valid(year, month, day)\n    target_jd = to_julian_day(year, month, day)\n    dow = day_of_week(year, month, day)\n    thursday_jd = target_jd + (3 - dow)\n    thursday_year, _, _ = from_julian_day(thursday_jd)\n    jan4_jd = to_julian_day(thursday_year, 1, 4)\n    jan4_dow = day_of_week(thursday_year, 1, 4)\n    week1_monday_jd = jan4_jd - jan4_dow\n    monday_jd = target_jd - dow\n    return (monday_jd - week1_monday_jd) // 7 + 1\n__all__ = ['Date', 'MIN_YEAR', 'MAX_YEAR', 'WEEKDAYS', 'add_days', 'business_days_between', 'date_diff', 'day_of_week', 'days_in_month', 'format_iso', 'from_julian_day', 'from_ordinal', 'is_business_day', 'is_leap_year', 'is_valid_date', 'is_weekend', 'iso_week_number', 'next_business_day', 'ordinal_day', 'parse_iso_date', 'previous_business_day', 'to_julian_day', 'weekday_name']"
    }
  ],
  "module_summary": "Proleptic Gregorian calendar helpers.\ndef is_leap_year(year: int): ...\ndef days_in_month(year: int, month: int): ...\ndef is_valid_date(year: int, month: int, day: int): ...\ndef _require_valid(year: int, month: int, day: int): ...\ndef parse_iso_date(s: str): ...\ndef format_iso(year: int, month: int, day: int): ...\ndef ordinal_day(year: int, month: int, day: int): ...\ndef from_ordinal(year: int, ordinal: int): ...\ndef to_julian_day(year: int, month: int, day: int): ...\ndef from_julian_day(jd: int): ...\ndef add_days(year: int, month: int, day: int, delta: int): ...\ndef date_diff(d1: Date, d2: Date): ...\ndef day_of_week(year: int, month: int, day: int): ...\ndef weekday_name(year: int, month: int, day: int): ...\ndef is_weekend(year: int, month: int, day: int): ...\ndef is_business_day(year: int, month: int, day: int): ...\ndef next_business_day(year: int, month: int, day: int): ...\ndef previous_business_day(year: int, month: int, day: int): ...\ndef business_days_between(d1: Date, d2: Date): ...\ndef iso_week_number(year: int, month: int, day: int): ...",
  "existing_test_names": [
    "tests/test_parser.py::test_parse_iso_date_basic",
    "tests/test_parser.py::test_format_iso_basic",
    "tests/test_parser.py::test_is_leap_year_2024",
    "tests/test_parser.py::test_is_leap_year_2023",
    "tests/test_parser.py::test_days_in_january",
    "tests/test_parser.py::test_days_in_april",
    "tests/test_parser.py::test_day_of_week_known_saturday",
    "tests/test_parser.py::test_weekday_name_known",
    "tests/test_parser.py::test_add_days_simple_positive",
    "tests/test_parser.py::test_date_diff_simple",
    "tests/test_parser.py::test_ordinal_day_january_first",
    "tests/test_parser.py::test_is_weekend_saturday",
    "tests/test_parser.py::test_is_business_day_wednesday",
    "tests/test_parser.py::test_next_business_day_from_wednesday",
    "tests/test_parser.py::test_business_days_between_within_week",
    "tests/test_parser.py::test_iso_week_number_mid_year"
  ]
}
