"""Self-curated mini-libs.

Each subdirectory is an independent, installable Python package with its own
deliberately weak test suite. The `mutant_hunter` package does NOT import from
these libs; they exist as targets for the mutation engine.
"""
